apiVersion: apps/v1
kind: Deployment
metadata:
  name: fintech-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fintech
  template:
    metadata:
      labels:
        app: fintech
    spec:
      containers:
      - name: fintech-container
        image: rey26341/fintech-app:latest
         imagePullPolicy: Always
        ports:
        - containerPort: 5000
